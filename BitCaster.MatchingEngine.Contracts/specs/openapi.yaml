openapi: 3.1.0
info:
  title: BitCaster Matching Engine API
  description: REST API for the BitCaster prediction market matching engine.
  version: 0.2.0
  license:
    name: MIT

servers:
  - url: http://localhost:5001
    description: Local development (via Aspire AppHost)

paths:
  /api/v1/markets/{conditionId}:
    post:
      operationId: createMarket
      summary: Register a new market on the matching engine
      description: >
        Validates the condition exists in the mint, creates empty order books
        with CPMM pools for each outcome, and optionally stores a thumbnail.
      parameters:
        - $ref: '#/components/parameters/ConditionId'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - metadata
              properties:
                metadata:
                  type: string
                  description: >
                    JSON-encoded CreateMarketRequest object containing market
                    title, description, outcomes, liquidity, and category tags.
                thumbnail:
                  type: string
                  format: binary
                  description: Optional thumbnail image (JPEG, PNG, or WebP, max 5 MB).
      responses:
        '200':
          description: Market created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateMarketResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                type: string
        '409':
          description: Market already exists for this condition
          content:
            application/json:
              schema:
                type: string

  /api/v1/{marketId}/orders:
    post:
      operationId: submitOrder
      summary: Submit a new order
      description: Submit a limit or market order to the matching engine.
      parameters:
        - $ref: '#/components/parameters/MarketId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitOrderRequest'
      responses:
        '200':
          description: Order accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitOrderResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                type: string

  /api/v1/{marketId}/orders/{orderId}:
    delete:
      operationId: cancelOrder
      summary: Cancel an existing order
      parameters:
        - $ref: '#/components/parameters/MarketId'
        - name: orderId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: The unique identifier of the order to cancel.
      responses:
        '200':
          description: Order cancelled
        '404':
          description: Order not found

  /api/v1/{marketId}/orderbook:
    get:
      operationId: getOrderBook
      summary: Get the current order book snapshot
      parameters:
        - $ref: '#/components/parameters/MarketId'
      responses:
        '200':
          description: Current order book
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderBookSnapshot'

  /api/v1/{marketId}/liquidity:
    get:
      operationId: getLiquidity
      summary: Get CPMM pool state
      description: Returns the current state of the CPMM liquidity pool for a market.
      parameters:
        - $ref: '#/components/parameters/MarketId'
      responses:
        '200':
          description: Current pool state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiquidityStateResponse'
        '404':
          description: No CPMM pool for this market

  /api/v1/{marketId}/metadata:
    get:
      operationId: getMarketMetadata
      summary: Get market metadata
      description: >
        Returns aggregated metadata for a market including trading volume,
        trade count, unique trader count, liquidity, and like count.
      parameters:
        - $ref: '#/components/parameters/MarketId'
        - name: userId
          in: query
          required: false
          schema:
            type: string
          description: Optional user ID to check if the user has liked this market.
      responses:
        '200':
          description: Market metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MarketMetadataSnapshot'

  /api/v1/{conditionId}/thumbnail:
    get:
      operationId: getThumbnail
      summary: Get the thumbnail image for a condition
      parameters:
        - $ref: '#/components/parameters/ConditionId'
      responses:
        '200':
          description: The thumbnail image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          description: No thumbnail for this condition

  /api/v1/{marketId}/like:
    post:
      operationId: toggleMarketLike
      summary: Toggle market like
      description: >
        Toggles the like state for a user on a market. If the user has
        already liked the market, the like is removed.
      parameters:
        - $ref: '#/components/parameters/MarketId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ToggleLikeRequest'
      responses:
        '200':
          description: Updated like state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToggleLikeResponse'

components:
  parameters:
    MarketId:
      name: marketId
      in: path
      required: true
      schema:
        type: string
      description: >
        The market to trade on, in the format "{conditionId}-{outcomeName}"
        (e.g. "deadbeef…abc-Alice"). Each outcome of a condition has its own
        independent binary order book. A marketId containing "|" is invalid.

    ConditionId:
      name: conditionId
      in: path
      required: true
      schema:
        type: string
      description: >
        The condition identifier (hex string derived from the oracle announcement).

  schemas:
    Sats:
      type: integer
      format: int64
      minimum: 0
      description: A non-negative amount of satoshis.

    Probability:
      type: integer
      minimum: 1
      maximum: 99
      description: A probability price representing a percentage chance [1, 99].

    OrderSide:
      type: string
      enum:
        - Buy
        - Sell
      description: Direction of an order relative to the outcome token.

    OrderType:
      type: string
      enum:
        - Limit
        - Market
      description: Execution semantics of an order.

    TimeInForce:
      type: string
      enum:
        - GTC
        - FOK
        - FAK
      description: >
        Time-in-force policy. GTC = Good-Till-Cancel (rests on book),
        FOK = Fill-Or-Kill (reject if not fully filled),
        FAK = Fill-And-Kill (cancel remaining after partial fill).

    MatchPath:
      type: string
      enum:
        - Direct
        - Complementary
      description: How two orders were matched together.

    Fill:
      type: object
      required:
        - id
        - takerOrderId
        - makerOrderId
        - amountSats
        - executionPrice
        - path
        - filledAt
      properties:
        id:
          type: string
          format: uuid
          description: Unique fill identifier.
        takerOrderId:
          type: string
          format: uuid
          description: The incoming (aggressor) order that triggered this fill.
        makerOrderId:
          type: string
          format: uuid
          description: The resting order that was matched against.
        amountSats:
          $ref: '#/components/schemas/Sats'
        executionPrice:
          $ref: '#/components/schemas/Probability'
        path:
          $ref: '#/components/schemas/MatchPath'
        filledAt:
          type: string
          format: date-time
          description: Timestamp when this fill was executed.

    SubmitOrderRequest:
      type: object
      required:
        - outcomeId
        - side
        - price
        - amountSats
        - userId
      properties:
        outcomeId:
          type: string
          description: The outcome to trade (e.g. "Alice", "YES").
        side:
          $ref: '#/components/schemas/OrderSide'
        price:
          $ref: '#/components/schemas/Probability'
        amountSats:
          $ref: '#/components/schemas/Sats'
        userId:
          type: string
          description: Opaque user identifier (e.g. Nostr pubkey hex).
        timeInForce:
          $ref: '#/components/schemas/TimeInForce'
          default: GTC

    SubmitOrderResponse:
      type: object
      required:
        - orderId
        - status
        - remainingAmountSats
        - fills
      properties:
        orderId:
          type: string
          format: uuid
          description: The unique identifier assigned to this order.
        status:
          type: string
          description: >
            One of: "filled", "partially_filled", "resting", "cancelled".
        remainingAmountSats:
          $ref: '#/components/schemas/Sats'
        fills:
          type: array
          items:
            $ref: '#/components/schemas/Fill'
          description: List of fills produced by this order. Empty if no matches.

    LevelDto:
      type: object
      required:
        - price
        - amount
      properties:
        price:
          $ref: '#/components/schemas/Probability'
        amount:
          $ref: '#/components/schemas/Sats'
      description: A single price level in the order book depth.

    OrderBookSnapshot:
      type: object
      required:
        - marketId
        - bids
        - asks
      properties:
        marketId:
          type: string
          description: >
            The market ID in the format "{conditionId}-{outcomeName}".
        bids:
          type: array
          items:
            $ref: '#/components/schemas/LevelDto'
          description: Buy-side levels sorted by price descending (best bid first).
        asks:
          type: array
          items:
            $ref: '#/components/schemas/LevelDto'
          description: Sell-side levels sorted by price ascending (best ask first).
        spread:
          oneOf:
            - type: integer
            - type: 'null'
          description: Difference between best ask and best bid. Null if either side is empty.

    CreateMarketOutcome:
      type: object
      required:
        - name
        - probability
      properties:
        name:
          type: string
          description: Outcome label (e.g. "Yes", "Alice").
        probability:
          type: integer
          minimum: 1
          maximum: 99
          description: Initial implied probability for this outcome [1, 99].

    CreateMarketRequest:
      type: object
      description: >
        JSON payload embedded in the multipart `metadata` field of the
        createMarket endpoint.
      required:
        - title
        - description
        - outcomes
      properties:
        title:
          type: string
          description: Human-readable market title.
        description:
          type: string
          description: Detailed market description.
        outcomes:
          type: array
          items:
            $ref: '#/components/schemas/CreateMarketOutcome'
          minItems: 2
          description: The outcomes for the market (at least 2).
        liquiditySats:
          type: integer
          format: int64
          minimum: 0
          default: 0
          description: Initial liquidity to seed across outcome CPMM pools (in sats).
        categoryTags:
          type: array
          items:
            type: string
          description: Optional category tags for the market.

    CreateMarketResponse:
      type: object
      required:
        - conditionId
        - marketsCreated
      properties:
        conditionId:
          type: string
          description: The condition ID this market was registered for.
        marketsCreated:
          type: array
          items:
            type: string
          description: >
            List of per-outcome market IDs created
            (format: "{conditionId}-{outcomeName}").
        thumbnailUrl:
          oneOf:
            - type: string
            - type: 'null'
          description: URL to the uploaded thumbnail, or null if none was provided.

    LiquidityStateResponse:
      type: object
      required:
        - marketId
        - reserveA
        - reserveB
        - impliedProbability
        - totalLiquiditySats
        - activeOrders
      properties:
        marketId:
          type: string
        reserveA:
          type: integer
          format: int64
        reserveB:
          type: integer
          format: int64
        impliedProbability:
          type: integer
          minimum: 1
          maximum: 99
        totalLiquiditySats:
          type: integer
          format: int64
        activeOrders:
          type: integer
          description: Number of active CPMM orders on the CLOB.

    MarketMetadataSnapshot:
      type: object
      required:
        - marketId
        - totalVolumeSats
        - totalTrades
        - uniqueTraderCount
        - totalLiquiditySats
        - likeCount
        - isLiked
      properties:
        marketId:
          type: string
          description: The market ID.
        totalVolumeSats:
          type: integer
          format: int64
          description: Total trading volume in satoshis across all fills.
        totalTrades:
          type: integer
          description: Total number of fills (trades) executed.
        uniqueTraderCount:
          type: integer
          description: Number of unique user IDs that have submitted orders.
        totalLiquiditySats:
          type: integer
          format: int64
          description: Total CPMM liquidity deposited in satoshis.
        likeCount:
          type: integer
          description: Number of users who have liked this market.
        isLiked:
          type: boolean
          description: Whether the requesting user has liked this market.

    ToggleLikeRequest:
      type: object
      required:
        - userId
      properties:
        userId:
          type: string
          description: The user toggling the like.

    ToggleLikeResponse:
      type: object
      required:
        - likeCount
        - isLiked
      properties:
        likeCount:
          type: integer
          description: Updated total like count for the market.
        isLiked:
          type: boolean
          description: Whether the user now likes the market.

